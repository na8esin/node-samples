const array1 = ['limit','exuberant', 'false']
const words = ['spray', 'limit', 'elite', 'exuberant', 'destruction', 'present'];

const result = words.filter(word => array1.includes(word));

console.log(result);